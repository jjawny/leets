# StackItem = tuple[int, int] # (idx, val)
# stack: List[StackItem] = []

# for currIdx, val in enumerate(arr):
#     while stack and condition(val, stack[-1]):
#         prevIdx, val = stack.pop()
#         # ^ do something w these
#     stack.append((currIdx, val))